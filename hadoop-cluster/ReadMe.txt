This folder contains two folders

1) Master: which contain hadoop folder and it is configured according to Master.
2) Slave: which contain hadoop folder and it is configured according to Slave.

hadoop folder from Master folder is copy into master node. 
hadoop folder from Slave folder is copy into slave node.

both folder copy into "/usr/local/" directory.
